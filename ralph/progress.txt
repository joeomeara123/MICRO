# Ralph Progress Log
Started: January 11, 2026
Branch: ralph/03-task-data-model

## Codebase Patterns

- Supabase client is in services/supabase.ts
- Supabase CLI must be installed via Homebrew (`brew install supabase/tap/supabase`), not npm global
- Migrations go in supabase/migrations/ with timestamp prefix (YYYYMMDDHHMMSS_description.sql)
- Auth context is in contexts/AuthContext.tsx
- Task/CardTask types are in types/task.ts, database types in types/database.ts
- Task service layer in services/tasks/ - all database ops go through service functions
- Service functions return TaskServiceResult<T> with {data, error} pattern
- Use `npx tsc --noEmit` for typecheck
- RLS pattern: `auth.uid() = user_id`
- Reusable `update_updated_at_column()` trigger function defined in connected_apps migration - use it for other tables
- Server-side-only tables: enable RLS with NO policies = clients cannot access, only Edge Functions with service_role key can
- Partial indexes with WHERE clause (e.g., `WHERE deleted_at IS NULL`) are more efficient for queries that always filter by that condition
- Soft delete pattern: use `deleted_at TIMESTAMPTZ` column, filter with `WHERE deleted_at IS NULL` in queries

---

## Pre-Implementation Notes

PRD 03: Task Data Model
- 10 user stories to complete
- Creates: connected_apps, oauth_tokens, tasks tables
- Generates TypeScript types from schema
- Connects feed to real database

---

## 2026-01-11 18:57 - US-001: Create Supabase Local Development Setup

**What was implemented:**
- Installed Supabase CLI v2.67.1 via Homebrew
- Initialized Supabase with `supabase init`
- Created migrations/ folder for SQL migrations
- Added README.md with migration workflow documentation

**Files changed:**
- supabase/ (new folder)
- supabase/config.toml (auto-generated)
- supabase/.gitignore (auto-generated with .temp/ exclusion)
- supabase/README.md (created)
- supabase/migrations/ (created)

**Learnings for future iterations:**
- `npm install -g supabase` does NOT work - use Homebrew instead
- Supabase init creates .gitignore with .temp/ already excluded
- config.toml uses project name from folder (Micro-app)

---

## 2026-01-11 19:05 - US-002: Create Database Enums

**What was implemented:**
- Created migration file with all 5 PostgreSQL enums
- integration_provider: google, notion, slack, linear, jira
- connection_status: active, token_expired, refresh_failed, revoked, disconnected
- task_source: notion, email, slack, ai_suggestion, manual
- task_priority: high, medium, low
- task_status: pending, approved, dismissed, snoozed

**Files changed:**
- supabase/migrations/20260111000001_create_enums.sql (new)

**Learnings for future iterations:**
- Migration files are plain SQL with CREATE TYPE syntax
- Enum values should match what the app UI/logic expects
- These enums must be created BEFORE any tables that reference them

**Note:** Migration must be applied via Supabase dashboard (US-006) before enums are visible.

---

## 2026-01-11 19:15 - US-003: Create connected_apps Table

**What was implemented:**
- Created migration file for connected_apps table
- Table tracks OAuth integrations per user (Google, Notion, Slack, etc.)
- UUID primary key with auto-generation via gen_random_uuid()
- Foreign key to auth.users with ON DELETE CASCADE
- Provider and status columns use enums from US-002
- Provider account fields for storing OAuth profile info
- granted_scopes array for OAuth permissions
- Unique constraint on (user_id, provider) prevents duplicates
- Auto-update trigger for updated_at column (reusable function)
- RLS enabled with 4 policies (SELECT/INSERT/UPDATE/DELETE for own rows)

**Files changed:**
- supabase/migrations/20260111000002_create_connected_apps.sql (new)

**Learnings for future iterations:**
- The `update_updated_at_column()` function is reusable - other tables can use it
- RLS pattern uses `auth.uid() = user_id` for USING and `WITH CHECK` clauses
- UPDATE policy needs both USING (filter existing rows) and WITH CHECK (validate new values)
- gen_random_uuid() is the PostgreSQL built-in for UUID generation (no extension needed)

**Note:** Migration must be applied via Supabase dashboard (US-006) before table is visible.

---

## 2026-01-11 19:25 - US-004: Create oauth_tokens Table (Server-Side Only)

**What was implemented:**
- Created migration file for oauth_tokens table
- Table stores OAuth access and refresh tokens securely
- UUID primary key with auto-generation
- Foreign key to connected_apps with ON DELETE CASCADE
- Index on connected_app_id for fast lookups
- Reuses existing update_updated_at_column() trigger function
- RLS enabled with NO policies (critical security feature)
- Only Supabase Edge Functions with service_role key can access

**Files changed:**
- supabase/migrations/20260111000003_create_oauth_tokens.sql (new)

**Learnings for future iterations:**
- RLS enabled + no policies = table is completely inaccessible from client
- This pattern is used for any server-side-only data (tokens, secrets, etc.)
- The update_updated_at_column() function is already defined in connected_apps migration - just reference it
- Comments in SQL help document security decisions for future developers

**Note:** Migration must be applied via Supabase dashboard (US-006) before table is visible.

---

## 2026-01-11 19:35 - US-005: Create tasks Table

**What was implemented:**
- Created migration file for tasks table (core table for the app)
- UUID primary key with auto-generation via gen_random_uuid()
- Foreign key to auth.users with ON DELETE CASCADE
- Typed columns using enums from US-002: source (task_source), priority_level (task_priority), status (task_status)
- JSONB metadata column for source-specific flexibility
- Lifecycle columns: snoozed_until, actioned_at, deleted_at (soft delete pattern)
- Two strategic indexes:
  - idx_tasks_user_feed: partial index on (user_id, status) WHERE deleted_at IS NULL for efficient feed queries
  - idx_tasks_deduplication: on (user_id, source, source_id) to prevent duplicate imports
- Reuses update_updated_at_column() trigger from connected_apps migration
- RLS enabled with 4 policies (SELECT/INSERT/UPDATE/DELETE for own rows)

**Files changed:**
- supabase/migrations/20260111000004_create_tasks.sql (new)

**Learnings for future iterations:**
- Partial indexes with WHERE clauses are more efficient for queries that always filter by that condition
- Soft delete pattern (deleted_at IS NULL) keeps data for audit while filtering from active queries
- The hybrid pattern (typed columns + JSONB metadata) provides both type safety and flexibility
- Each source type (Notion, Email, Slack) will store its specific data in the metadata JSONB column

**Note:** Migration must be applied via Supabase dashboard (US-006) before table is visible.

---

## 2026-01-11 19:12 - US-006: Apply Migrations to Supabase (BLOCKED)

**Status: BLOCKED - Requires manual user action**

**What needs to happen:**
1. Open Supabase dashboard: https://supabase.com/dashboard/project/etwdxydfcxlqhbqzzdqs/sql
2. Copy contents of `supabase/migrations/_apply_all.sql`
3. Paste into SQL editor and click "Run"
4. Run verification queries (uncomment at bottom of file)

**Alternatively, authenticate CLI:**
```bash
supabase login
supabase link --project-ref etwdxydfcxlqhbqzzdqs
supabase db push
```

**Files ready:**
- supabase/migrations/_apply_all.sql - consolidated SQL for manual application
- All 4 individual migration files in supabase/migrations/

**Typecheck:** Passes (npx tsc --noEmit)

**Cannot complete autonomously because:**
- Supabase CLI not authenticated (requires `supabase login`)
- Dashboard application requires browser interaction

**Next:** Once user confirms migrations are applied, verify enums/tables exist and mark US-006 complete.

---

## 2026-01-11 19:45 - US-008: Create Task TypeScript Interface with Metadata Types

**What was implemented:**
- Created types/database.ts with manual types matching database schema
- Created types/task.ts with source-specific metadata types
- Defined NotionTaskMetadata, EmailTaskMetadata, SlackTaskMetadata interfaces
- Added AISuggestionMetadata and ManualTaskMetadata for completeness
- Created TaskMetadata union type combining all interfaces
- Added CardTask interface for UI components (handles priority case conversion)
- Added type guards: isNotionTask(), isEmailTask(), isSlackTask(), etc.
- Added helper functions: priorityToDisplay(), displayToPriority(), taskToCardTask()
- Updated TaskCard.tsx to import CardTask from types/task.ts
- Re-exported Task type alias for backward compatibility

**Files changed:**
- types/database.ts (new) - Database types matching schema
- types/task.ts (new) - Task interface with metadata types
- components/TaskCard.tsx (modified) - Import from types/task.ts

**Learnings for future iterations:**
- Database types use lowercase enums (high, medium, low), UI uses capitalized (High, Medium, Low)
- CardTask type adds UI-specific fields (brand, owner) not in database
- Type guards enable discriminated unions for source-specific metadata handling
- Re-exporting types maintains backward compatibility with existing imports

---

## 2026-01-11 19:55 - US-009: Create Task Service Layer

**What was implemented:**
- Created services/tasks/ folder
- Created services/tasks/task-service.ts with CRUD operations
- TaskServiceResult<T> type for consistent error handling
- FetchTasksOptions interface with filtering options
- Functions: fetchUserTasks, updateTaskStatus, snoozeTask, createTask, deleteTask
- All functions return {data, error} pattern
- Console logging at entry/exit points for debugging
- Uses untyped Supabase client (types will be generated in US-007)
- Created services/tasks/index.ts for clean exports
- Created services/tasks/CLAUDE.md with usage documentation
- Updated types/database.ts with Json type for JSONB columns

**Files changed:**
- services/tasks/task-service.ts (new) - Task CRUD service layer
- services/tasks/index.ts (new) - Public exports
- services/tasks/CLAUDE.md (new) - Service documentation
- services/supabase.ts (modified) - Removed Database generic until types generated
- types/database.ts (modified) - Added Json type, Relationships, Views/Functions/CompositeTypes

**Learnings for future iterations:**
- Supabase client with Database generic requires exact type match
- Pragmatic solution: use untyped client, add types after US-007 generates them
- TaskServiceResult pattern makes error handling consistent across app
- Console logging with [TaskService] prefix aids debugging
- Import task service via: `import { fetchUserTasks, updateTaskStatus } from '@/services/tasks'`

---

## 2026-01-11 20:05 - US-010: Connect Feed to Database with Sample Fallback

**What was implemented:**
- Feed screen imports fetchUserTasks and updateTaskStatus from task service
- useAuth hook provides user ID for database queries
- useEffect fetches tasks when user becomes available
- Loading state with ActivityIndicator while fetching
- Sample fallback when database is empty or user not authenticated
- Error state with retry button and "Use Sample Data" option
- handleSwipeLeft calls updateTaskStatus(taskId, 'dismissed')
- handleSwipeRight calls updateTaskStatus(taskId, 'approved')
- Console logging confirms all database operations
- Header shows "(Sample)" indicator when using fallback data

**Files changed:**
- app/(tabs)/index.tsx (modified) - Connected to task service with states

**Learnings for future iterations:**
- taskToCardTask() converts database Task to UI CardTask format
- usingSampleData flag prevents unnecessary database calls for sample tasks
- useCallback for loadTasks prevents re-creation on each render
- Graceful degradation: error → sample fallback maintains app usability

---

## PRD 03 Summary

**Completed Stories:** 8 of 10 (80%)
- US-001: Supabase Local Development Setup ✅
- US-002: Database Enums ✅
- US-003: connected_apps Table ✅
- US-004: oauth_tokens Table ✅
- US-005: tasks Table ✅
- US-006: Apply Migrations (BLOCKED - requires manual action)
- US-007: Generate TypeScript Types (BLOCKED - requires US-006)
- US-008: Task TypeScript Interface ✅
- US-009: Task Service Layer ✅
- US-010: Connect Feed to Database ✅

**Remaining work:**
1. US-006: Apply migrations via Supabase dashboard (manual step)
2. US-007: Run `supabase gen types typescript` after migrations applied

**Next PRD:** 04-Notion-Sync (after US-006 and US-007 complete)

---
